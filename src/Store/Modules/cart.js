const state = {
   cartItems:[]
}
const getters = {
    cart: (state) => state.cartItems,
}
const actions = {
    
}
const mutations = {
    addToCart(context,payload){
        
    }
}
export default {
    state,
    getters,
    actions,
    mutations,

}